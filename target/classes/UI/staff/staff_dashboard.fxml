<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.ucop.controller.staff.StaffDashboardController"
            prefWidth="1200" prefHeight="700">

    <top>
        <VBox spacing="0">

            <!-- HEADER -->
            <HBox style="-fx-background-color:#0d47a1; -fx-padding:15;" spacing="20">
                <Label text="UCOP — STAFF PORTAL"
                       style="-fx-text-fill:white; -fx-font-size:22px;" />

                <Pane HBox.hgrow="ALWAYS"/>

                <Label fx:id="lblStaff"
                       text="Welcome, Staff"
                       style="-fx-text-fill:white;" />

                <Button text="Logout"
                        onAction="#logout"
                        style="-fx-background-color:#e53935; -fx-text-fill:white;" />
            </HBox>

            <!-- STAFF MENU -->
            <HBox spacing="20" style="-fx-background-color:#1565c0; -fx-padding:12;">
                <Button text="📦 Orders"         onAction="#openOrders"        styleClass="menu-btn"/>
                <Button text="🎫 Tickets"        onAction="#openTickets"       styleClass="menu-btn"/>
                <Button text="🏬 Inventory"      onAction="#openWarehouse"     styleClass="menu-btn"/>
                <Button text="💰 Payments"       onAction="#openPayments"      styleClass="menu-btn"/>
                <Button text="🚚 Shipment"       onAction="#openShipment"      styleClass="menu-btn"/>
                <Button text="↩ Refunds"         onAction="#openRefunds"       styleClass="menu-btn"/>
            </HBox>

        </VBox>
    </top>

    <center>
        <StackPane fx:id="contentArea" style="-fx-padding:20;">
            <Label text="Staff Dashboard — Manage Operations"
                   style="-fx-font-size:20px;"/>
        </StackPane>
    </center>

</BorderPane>
