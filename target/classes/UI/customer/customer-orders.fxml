<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.ucop.controller.customer.CustomerOrderController">
    
    <!-- Top Bar -->
    <top>
        <VBox>
            <HBox alignment="CENTER_LEFT" spacing="10" style="-fx-background-color: #009688; -fx-padding: 15;">
                <Label text="📋 Đơn Hàng Của Tôi" textFill="white">
                    <font><Font name="System Bold" size="20"/></font>
                </Label>
            </HBox>
            
            <!-- Filter Bar -->
            <HBox spacing="15" style="-fx-padding: 15; -fx-background-color: #f5f5f5;">
                <TextField fx:id="searchField" promptText="Tìm theo mã đơn hàng..." prefWidth="250"/>
                <ComboBox fx:id="statusFilter" promptText="Trạng thái" prefWidth="150"/>
                <Button text="🔍 Tìm" style="-fx-background-color: #2196F3; -fx-text-fill: white;"/>
                <Button text="🔄 Làm mới" style="-fx-background-color: #4CAF50; -fx-text-fill: white;"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Label fx:id="totalOrdersLabel" text="Tổng: 0 đơn hàng"/>
            </HBox>
        </VBox>
    </top>
    
    <!-- Center - Orders Table -->
    <center>
        <VBox spacing="10" style="-fx-padding: 20;">
            <!-- Orders Table -->
            <TableView fx:id="orderTable" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="orderNumberColumn" text="Mã đơn hàng" prefWidth="150"/>
                    <TableColumn fx:id="orderDateColumn" text="Ngày đặt" prefWidth="150"/>
                    <TableColumn fx:id="statusColumn" text="Trạng thái" prefWidth="150"/>
                    <TableColumn fx:id="totalColumn" text="Tổng tiền" prefWidth="150"/>
                </columns>
            </TableView>
            
            <!-- Order Details -->
            <VBox spacing="10" style="-fx-border-color: #e0e0e0; -fx-border-width: 1; -fx-padding: 15; -fx-background-color: #fafafa;">
                <Label text="Chi tiết đơn hàng:" style="-fx-font-weight: bold; -fx-font-size: 14px;"/>
                <TextArea fx:id="orderDetailsArea" editable="false" wrapText="true" prefRowCount="8"/>
                <HBox spacing="10" alignment="CENTER_RIGHT">
                    <Button text="Xem chi tiết" onAction="#handleViewDetails" style="-fx-background-color: #2196F3; -fx-text-fill: white;"/>
                    <Button text="Hủy đơn" onAction="#handleCancelOrder" style="-fx-background-color: #f44336; -fx-text-fill: white;"/>
                </HBox>
            </VBox>
        </VBox>
    </center>
    
    <!-- Bottom - Status Bar -->
    <bottom>
        <HBox alignment="CENTER_LEFT" spacing="10" style="-fx-background-color: #e0e0e0; -fx-padding: 8;">
            <Label text="Trạng thái:"/>
            <Label text="Sẵn sàng" style="-fx-text-fill: green;"/>
        </HBox>
    </bottom>
    
</BorderPane>
