<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.ucop.controller.admin.AdminDashboardController"
            prefWidth="1200" prefHeight="700">

    <top>
        <VBox spacing="0">

            <!-- HEADER -->
            <HBox spacing="20" alignment="CENTER_LEFT"
                  style="-fx-background-color:#1e2a38; -fx-padding:15;">
                <Label text="UCOP — ADMIN PORTAL"
                       style="-fx-text-fill:white; -fx-font-size:24px; -fx-font-weight:bold;"/>

                <Pane HBox.hgrow="ALWAYS" />

                <Label fx:id="lblAdmin" text="Hello, Admin"
                       style="-fx-text-fill:white; -fx-font-size:14px;" />

                <Button text="Logout"
                        onAction="#logout"
                        style="-fx-background-color:#e74c3c; -fx-text-fill:white;" />
            </HBox>

            <!-- MAIN MENU -->
            <HBox spacing="20" alignment="CENTER_LEFT"
                  style="-fx-background-color:#2c3e50; -fx-padding:12;">

                <Button text="👤 Users"               onAction="#openUserMgmt"
                        style="-fx-background-color:transparent; -fx-text-fill:white;" />

                <Button text="🛡 Roles"               onAction="#openRoleMgmt"
                        style="-fx-background-color:transparent; -fx-text-fill:white;" />

                <Button text="⚙ Tax / Shipping / Payment"  onAction="#openConfig"
                        style="-fx-background-color:transparent; -fx-text-fill:white;" />

                <Button text="🏷 Promotions"         onAction="#openPromotion"
                        style="-fx-background-color:transparent; -fx-text-fill:white;" />

                <Button text="📊 Reports"             onAction="#openReports"
                        style="-fx-background-color:transparent; -fx-text-fill:white;" />

                <Button text="📝 Audit Logs"          onAction="#openAudit"
                        style="-fx-background-color:transparent; -fx-text-fill:white;" />

                <Button text="🔐 Change Password"     onAction="#openChangePassword"
                        style="-fx-background-color:transparent; -fx-text-fill:white;" />
            </HBox>

        </VBox>
    </top>

    <center>
        <StackPane fx:id="contentArea" style="-fx-padding:20;">
            <Label text="Welcome Admin — Full System Control"
                   style="-fx-font-size:22px;"/>
        </StackPane>
    </center>

</BorderPane>
