<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.ucop.controller.staff.StaffPromotionController">
    
    <!-- Top Bar -->
    <top>
        <VBox>
            <HBox alignment="CENTER_LEFT" spacing="10" style="-fx-background-color: #9C27B0; -fx-padding: 15;">
                <Label text="🎁 Danh Sách Khuyến Mãi" textFill="white">
                    <font><Font name="System Bold" size="20"/></font>
                </Label>
                <Label text="(Chỉ xem)" textFill="#FFD700" style="-fx-font-style: italic;">
                    <font><Font size="14"/></font>
                </Label>
            </HBox>
            
            <!-- Search and Filter Bar -->
            <HBox spacing="15" style="-fx-padding: 15; -fx-background-color: #f5f5f5;">
                <TextField fx:id="txtSearch" promptText="Tìm kiếm mã khuyến mãi..." prefWidth="250"/>
                <ComboBox fx:id="cboDiscountType" promptText="Loại giảm giá" prefWidth="150"/>
                <ComboBox fx:id="cboStatus" promptText="Trạng thái" prefWidth="120"/>
                <Button text="🔍 Tìm kiếm" onAction="#handleSearch" style="-fx-background-color: #2196F3; -fx-text-fill: white;"/>
                <Button text="🔄 Làm mới" onAction="#handleRefresh" style="-fx-background-color: #4CAF50; -fx-text-fill: white;"/>
            </HBox>
        </VBox>
    </top>
    
    <!-- Center - Table -->
    <center>
        <VBox style="-fx-padding: 15;">
            <TableView fx:id="tablePromotions" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="colCode" text="Mã KM" prefWidth="120"/>
                    <TableColumn fx:id="colName" text="Tên KM" prefWidth="220"/>
                    <TableColumn fx:id="colType" text="Loại" prefWidth="120"/>
                    <TableColumn fx:id="colValue" text="Giá trị" prefWidth="100"/>
                    <TableColumn fx:id="colMinAmount" text="Đơn tối thiểu" prefWidth="130"/>
                    <TableColumn fx:id="colMaxDiscount" text="Giảm tối đa" prefWidth="130"/>
                    <TableColumn fx:id="colStartDate" text="Ngày bắt đầu" prefWidth="130"/>
                    <TableColumn fx:id="colEndDate" text="Ngày kết thúc" prefWidth="130"/>
                    <TableColumn fx:id="colUsageCount" text="Lượt dùng" prefWidth="100"/>
                    <TableColumn fx:id="colMaxUsage" text="Giới hạn" prefWidth="100"/>
                    <TableColumn fx:id="colActive" text="Trạng thái" prefWidth="110"/>
                </columns>
            </TableView>
            
            <!-- Pagination -->
            <HBox alignment="CENTER" spacing="10" style="-fx-padding: 10;">
                <Button fx:id="btnFirst" text="⏮ Đầu" onAction="#handleFirst"/>
                <Button fx:id="btnPrev" text="◀ Trước" onAction="#handlePrevious"/>
                <Label fx:id="lblPage" text="Trang 1 / 1"/>
                <Button fx:id="btnNext" text="Sau ▶" onAction="#handleNext"/>
                <Button fx:id="btnLast" text="Cuối ⏭" onAction="#handleLast"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Label text="Hiển thị:"/>
                <ComboBox fx:id="cboPageSize" prefWidth="80"/>
                <Label fx:id="lblTotal" text="Tổng: 0 khuyến mãi"/>
            </HBox>
        </VBox>
    </center>
    
    <!-- Bottom Status Bar -->
    <bottom>
        <HBox alignment="CENTER_LEFT" spacing="10" style="-fx-background-color: #f0f0f0; -fx-padding: 8;">
            <Label text="Trạng thái:"/>
            <Label fx:id="lblStatus" text="Sẵn sàng" style="-fx-text-fill: green;"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label text="⚠ Bạn chỉ có quyền xem, không thể thêm/sửa/xóa" style="-fx-text-fill: #FF9800; -fx-font-style: italic;"/>
        </HBox>
    </bottom>
    
</BorderPane>
